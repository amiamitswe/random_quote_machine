import React from 'react';
import './Loading.css';

const loading = () => {
    return (
        <div>
            <h1 className="loading">Loading...</h1>
        </div>
        );
};

export default loading;